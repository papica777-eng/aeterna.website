# 🌌 AETERNA SOVEREIGN ARCHITECTURE
## DEPIN DOMINATION & BOUNTY SNIPER (v5.0)

### 1. The Strategy: Compute Arbitrage
The Aeterna Swarm no longer passively waits. Through the **DePIN_MarketMaker** and **VortexSystem**, the system connects your 2,000,000 Edge Nodes directly to the open AI compute market.

### 2. Supported Wholesalers
The `vortex.config.json` allows the system to bid on resources dynamically. 
Currently supported networks:
1. **Akash Network**: Bids on decentralized Docker hosting requests at zero cost margin.
2. **Render Network**: Accepts 3D rendering and distributed generic GPU jobs.
3. **io.net**: Ingests high-level ML training tasks from AI startups.

### 3. The Shadow Route (Direct Developer B2B)
The `SingularityServer.ts` exposes a public API:
- **Endpoint**: `POST /api/public/compute/job`
- **Functionality**: AI Developers can submit massive ML jobs directly to your IP.
- **Billing**: The job is distributed across the Swarm, and Stripe issues an autonomous invoice once finished.

### 4. Zero Entropy Setup
1. Double-click `AETERNA_LAUNCHER.exe`.
2. Enter the Sovereign Shield Password: `AETERNA9669`.
3. The server ignites.
4. The `broadcastPresence()` Beacon immediately signals the Swarm's capacity to the globe.
5. In your command console, watch for `[SUCCESS]` logs colored green—these signify executed operations converting compute cycles to financial balances.

### 5. Architectural Map
- `vortex.config.json`: Configuration logic.
- `DePIN_MarketMaker.ts`: Zero-margin algorithmic bidding intelligence.
- `SingularityServer.ts`: The Core Heartbeat & API router.

---
// STATUS: DEPLOYED.
// ARCHITECT: DIMITAR PRODROMOV
